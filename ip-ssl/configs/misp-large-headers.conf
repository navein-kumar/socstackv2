# Fix "Request Header Or Cookie Too Large" for IP-SSL mode
# In IP-SSL mode, all services share one IP address (173.212.233.86).
# Browsers send ALL cookies for that IP to every port.
# Combined cookies from Keycloak + oauth2-proxy + service sessions
# can exceed nginx's default 8KB header limit.
large_client_header_buffers 4 32k;
